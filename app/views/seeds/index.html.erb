<%= render "shared/header"%>
<%= flash[:user_delete] %>


<div class="top-slide">
<h1>スライドショー導入予定</h1>

</div>
<div class="top-explanation">
  <h1>アプリの内容説明</h1>
  <p>
  業種に関わらず、業務展開の機会を提供するサービス。簡単に言えば、異業種のマッチングサービス。自社の売りと他社の売りを合わせることで、新たな業務展開の架け橋になります。
  </p>

</div>

<ul class='seed-lists'>
  <% @seeds.each do |seed| %>
    <li class='list'>
      <%= link_to seed_path(seed.id) do %>
        <div class="seed-title">
          <%= seed.title %>
        </div>
        <div class="seed-tag">
          <%= seed.tag.name %>
        </div>
        <div class="seed-image" >
          <%= image_tag seed.image.variant(resize: '300x300') if seed.image.attached? %>
        </div>
        <div class="seed-idea">
          <p><%= seed.idea %></p>
        </div>
      <% end %>
      <div class="seed-user">
        <%= link_to seed.user.name, user_path(seed.user.id) %>
      </div>
    </li>
  <% end %>
</ul>

